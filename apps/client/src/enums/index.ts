export enum Mode {
    Location = 'LOCATION',
    Custom = 'CUSTOM',
}

export enum ActionType {
    Reset = 'RESET',
    SetMode = 'SET_MODE',
    SetLocation = 'SET_LOCATION',
    SetAiDescription = 'SET_AI_DESCRIPTION',
    SetName = 'SET_NAME',
    SetImage = 'SET_IMAGE',
}
